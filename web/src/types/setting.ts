export type SortSettings = {
  by: string
  order: number
  group: boolean
}

export type ViewSettings = {
  as: string
  navigation: boolean
  preview: boolean
  extension: boolean
}

export type LanguageSettings = {
  language: string
}
