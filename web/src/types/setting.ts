export type SortSettings = {
  by: string
}

export type ViewSettings = {
  as: string
  navigation: boolean
  preview: boolean
  extension: boolean
}

export type LanguageSettings = {
  language: string
}
